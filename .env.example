# ============================================================
# GrabSter – Environment Configuration
# ============================================================
# Copy this file to .env and fill in the required values:
#
#   cp .env.example .env
#
# .env is git-ignored and should NEVER be committed.
# ============================================================

# ----------------------------------------------------------
# REQUIRED – Application Secret Key
# Generate a strong key with:  openssl rand -hex 32
# The app will refuse to start if this is missing or set
# to the placeholder value below.
# ----------------------------------------------------------
SECRET_KEY=CHANGE_ME

# ----------------------------------------------------------
# Server
# ----------------------------------------------------------
# Port to bind on the host machine
PORT=8001

# Enable debug mode (exposes /docs, /redoc, verbose logging)
# Set to false in production
DEBUG=false

# ----------------------------------------------------------
# Storage paths
# These paths are relative to the container's /app directory,
# or absolute paths to host-mounted volumes.
# ----------------------------------------------------------
# SQLite database file
DB_PATH=data/app.db

# Directory where yt-dlp saves downloaded files
DOWNLOAD_DIR=downloads

# Directory for converted video output
CONVERT_DIR=converted

# ----------------------------------------------------------
# Security
# ----------------------------------------------------------
# Set to true when serving behind HTTPS (enables Secure
# cookie flag and HSTS headers).
# Set to false for local HTTP development only.
COOKIE_SECURE=true
